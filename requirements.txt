# Python 3.12. Tested with the versions in the comments; the lower bounds are what the code needs.
numpy>=2.0            # 2.4.3
pandas>=2.2           # 2.3.3
scipy>=1.13           # 1.17.1
scikit-learn>=1.6     # 1.8.0  (HistGradientBoostingClassifier with categorical support)
lifelines>=0.29       # 0.30.3 (Cox, Aalen-Johansen, concordance)
statsmodels>=0.14     # 0.14.6 (mixed model for gradient slopes)
joblib>=1.4           # 1.5.3
threadpoolctl>=3      # 3.6.0
shap>=0.46            # 0.52.0
matplotlib>=3.8       # 3.10.9
pyarrow>=15           # 25.0.1 (parquet copies of the synthetic tables)
pillow>=10            # 12.1.1
tabulate>=0.9         # 0.10.0 (markdown tables in summarise.py)
pypdf>=4              # 6.11.0 (curve reconstruction from the published PDFs)
openpyxl>=3.1         # 3.1.5  (reads the provided .xlsx extracts, real_data_check only)
python-pptx>=1.0      # 1.0.2  (presentation/build_deck.py)
pytest>=8             # 9.0.3
jupyter>=1.0          # to run the notebooks
