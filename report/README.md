# Report

`main.pdf` is a concise (13-page) write-up of the whole submission for the presentation: clinical problem, study design, curve reconstruction, simulator and calibration, models, surveillance experiment, results, the real-data check, limitations. Sources are cited from `references.bib`, which mirrors `../references/README.md`.

Build (TeX Live with biber): `./build.sh` or `latexmk -pdf main.tex`. Figures in `figures/` are copies of `../notebooks/*/output/figures/`.
